require 'spec_helper'

describe BolingForBatches do
  it 'has a version number' do
    expect(BolingForBatches::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
